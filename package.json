{
  "name": "vulgar-wordle",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "A Wordle clone where every answer is crude. All rights reserved.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint .",
    "format": "prettier --write .",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "@eslint/js": "^9.9.0",
    "eslint": "^9.9.0",
    "gh-pages": "^6.1.1",
    "prettier": "^3.3.3",
    "vite": "^5.4.0"
  }
}
